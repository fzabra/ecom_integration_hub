{
  "version": 2,
  "buildCommand": "rm -rf dist && mkdir -p dist && cp -r apps/web/src/public/* dist/",
  "outputDirectory": "dist",
  "cleanUrls": true,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
